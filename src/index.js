import React from 'react';
import ReactDOM from 'react-dom/client';
/* import GoogleDriveUpload from './Subirdoc'; // Asegúrate de que 'Subirdoc' tiene exportación por defecto */
import GoogleDriveUploadxd from './Conphp';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <GoogleDriveUploadxd />
  </React.StrictMode>
);
